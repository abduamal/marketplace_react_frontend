// the fetchShops function creates and returns an actionObject
export function fetchShops(action) {
  // fetch('http://localhost:3000/apiv2/shops')
  // .then(response => response.json())
  // .then(data => console.log(data))
  return action
}

// the actionObject is then dispatched to shopReducer
    // - shopReducer will then return a new state with changes based on the actions set in shopReducer
// dispatch(actionObject)
